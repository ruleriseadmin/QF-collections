server_tokens off;

add_header X-Frame-Options "SAMEORIGIN";
add_header X-Content-Type-Options "nosniff";
add_header X-XSS-Protection "1; mode=block";

client_max_body_size 100M;

keepalive_requests 10000;

upstream collection_app {
    server collection_app:90;

    keepalive 16;
}

server {
    listen 80;
    listen [::]:80;

    server_name collectionapi.quickcred.com.ng;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    location / {
        proxy_pass http://collection_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

}

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;

#     ssl_certificate /etc/letsencrypt/live/collectionapi.quickcred.com.ng/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/collectionapi.quickcred.com.ng/privkey.pem;

#     server_name collectionapi.quickcred.com.ng;

#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header Host $http_host;
#     proxy_http_version 1.1;
#     proxy_set_header Connection "";

#     location / {
#         proxy_pass http://collection_app;
#     }
# }
